import { UsernamecolorDirective } from './usernamecolor.directive';

describe('UsernamecolorDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernamecolorDirective();
    expect(directive).toBeTruthy();
  });
});
